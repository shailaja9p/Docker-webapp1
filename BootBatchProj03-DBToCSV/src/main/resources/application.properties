#enable the batch on the startup of the application
spring.batch.job.enabled=false
#if tables are not there related to job repository activity
#then table should be initialized on application startuo 
spring.batch.jdbc.initialize-schema=always

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/ntspbms616db
spring.datasource.username=root
spring.datasource.password=root

